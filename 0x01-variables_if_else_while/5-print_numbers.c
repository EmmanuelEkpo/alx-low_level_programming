#include <stdio.h>
/**
 * main - Entry point
 * Return: Invariably 0 (Success)
 */

int main(void)
{
	int x = 0;

	int y = 10;

	while
		(x < y)
		{
			printf("%d", x);
			x++;
		}
	putchar('\n');
	return (0);
}
