#include <stdio.h>
/**
 * main - Program that prints the alphabet in lowercase
 * Return: Invariably 0 (Success)
 */

int main(void)
{
	char m;

	m = 'a';
	while
		(m <= 'z') {
			putchar(m);
			m++;
		}
	return (0);
}
