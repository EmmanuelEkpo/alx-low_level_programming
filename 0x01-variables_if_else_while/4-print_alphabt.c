#include <stdio.h>
/**
 * main - Entry point
 * Return: Invariably 0 (Success)
 */

int main(void)
{
	char m;

	char n;

	char o;

	m = 'a'++ <= 'z';
	n = 'q';
	o = 'e';
	while
		(m != n && m != o) {
			putchar(m);
		}
	putchar('\n');
	return (0);
}
