#include <stdio.h>
/**
 * main - Entry point
 * Return: Invariably 0 (Success)
 */

int main(void)
{
	char m;

	char n;

	char o;

	m = 'a';
	n = 'q';
	o = 'e';
	while
		((m != n && m != o) && m <= 'z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
